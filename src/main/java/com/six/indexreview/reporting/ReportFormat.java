package com.six.indexreview.reporting;

/** Supported review report output formats. Additional export formats can be
 * introduced here without changing the domain result model. */
public enum ReportFormat {
    JSON
}
